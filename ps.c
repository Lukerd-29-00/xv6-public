#include "types.h"
#include "user.h"
/** Makes the crsp system calla nd exits.
 * side effects: prints the names and pids of the running and sleeping processes on the machine. Exits the program.
 */
int main(void){
	crsp();
	exit();
}
